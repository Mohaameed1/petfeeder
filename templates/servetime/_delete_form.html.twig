<form method="post" action="{{ path('app_servetime_delete', {'id': servetime.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ servetime.id) }}">
    <button class="btn">Delete</button>
</form>
